import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'SafeFile Documents' };

export default function Page() {
  return (
    <PagePlaceholder
      title="SafeFile Documents"
      description="The six-module SafeFile register with validity and S.16(1) sign-off tracking."
      sprint="the document module"
    />
  );
}
