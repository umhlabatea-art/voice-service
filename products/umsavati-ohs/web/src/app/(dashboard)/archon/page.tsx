import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'ARCHON Command Centre' };

export default function Page() {
  return (
    <PagePlaceholder
      title="ARCHON Command Centre"
      description="Agent roster, HITL approval queue, and three-layer memory status."
      sprint="the ARCHON embed"
    />
  );
}
