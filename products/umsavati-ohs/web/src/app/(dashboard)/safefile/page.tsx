import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'SafeFile Generator' };

export default function Page() {
  return (
    <PagePlaceholder
      title="SafeFile Generator"
      description="Four-phase PDF/DOCX export pipeline with SA legislative templates."
      sprint="the generator rebuild"
    />
  );
}
