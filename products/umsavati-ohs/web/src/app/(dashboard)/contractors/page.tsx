import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'Contractors' };

export default function Page() {
  return (
    <PagePlaceholder
      title="Contractors"
      description="CIDB grading and CR 5 competency verification for the contractor marketplace."
      sprint="the contractor module"
    />
  );
}
