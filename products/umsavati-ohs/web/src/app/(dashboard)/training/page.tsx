import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'Training' };

export default function Page() {
  return (
    <PagePlaceholder
      title="Training"
      description="Induction, first aid, and toolbox-talk currency per person."
      sprint="the training module"
    />
  );
}
