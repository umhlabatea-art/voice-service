import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'Physical Agents' };

export default function Page() {
  return (
    <PagePlaceholder
      title="Physical Agents"
      description="Noise, vibration, thermal, and radiation readings under the Physical Agents Regulations 2025."
      sprint="the monitoring module"
    />
  );
}
