import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'Appointments' };

export default function Page() {
  return (
    <PagePlaceholder
      title="Appointments"
      description="OHS Act S.16(1)/S.16(2)/S.8(2) appointment chain, CR 5 and SASREA designations."
      sprint="the legal compliance module"
    />
  );
}
