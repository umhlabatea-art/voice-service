import { PagePlaceholder } from '@/components/page-placeholder';

export const metadata = { title: 'Incidents' };

export default function Page() {
  return (
    <PagePlaceholder
      title="Incidents"
      description="S.24 reporting and COID claims with 7-day/14-day deadline tracking."
      sprint="the incident module"
    />
  );
}
