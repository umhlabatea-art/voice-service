# ⚡ XRAY REPORT — SYSTEM (Full PRS Audit · Sprint 9 Gate)
**Status**: 🔴 RED
**Scanned**: 2026-07-20 00:45 UTC · Claude, under CEO Phase 1 mandate
**Method**: Live Supabase inspection (management API + read-only SQL), repo scan. No migrations executed (HITL honoured).

---

## PRS — Platform Readiness Score

**PRS = 26 / 100 → CRITICAL band (< 45)**
Gate threshold ≥ 85. **Sprint 9 remains GATED.**

| Dimension | Weight | Scored | Evidence |
|-----------|--------|--------|----------|
| Data layer & security | 25 | 8 | 2 tables live w/ RLS + auth.uid() policies — but ~8% of documented schema; 1 SECURITY DEFINER exposure |
| Application surface | 25 | 3 | 0 of 11 documented Edge Functions exist; mobile/web/SafeFile code not under version control anywhere visible |
| Infrastructure & monitoring | 20 | 4 | Backend found PAUSED (restored 2026-07-20); no alerting, no error tracking; VPS services unverifiable from session |
| Compliance & legal (POPIA) | 15 | 5 | Lawful-basis memo drafted + consent-ledger designed (this session); nothing decided or built |
| Revenue readiness | 15 | 6 | SafeFile Pro™ live on Gumroad (per KB); funnel not instrumented, nurture not built |

---

## Critical Issues (blocking — must fix before Sprint 9)

1. **Documentation ≠ reality.** The KB claims "25 tables · 66 RLS policies · 14 triggers · 11 Edge Functions." The `lpafkclumhhwsvgxrkwv` project actually contains **2 tables (`sites`, `floorplans`), 2 policies, 2 triggers, 0 Edge Functions, 0 rows**. The Umsavati OHS production database exists in **no project in this Supabase account** (account holds only "Archon n8n Backend" + unrelated "R0cket ai backend", both found INACTIVE). Either it lives in another account — locate and register it — or Sprints 1–8 "delivered" status is unsupported by evidence.
2. **The backend was paused.** `lpafkclumhhwsvgxrkwv` sat INACTIVE — nothing in production could have been reading or writing it. Restored to ACTIVE_HEALTHY during this audit. Free-tier projects re-pause after inactivity; if this backend matters, it needs a keep-alive or paid tier.
3. **No product source under version control.** `products/`, `agents/`, `infra/` were empty scaffolds until this session. A platform that exists only on one machine is one disk failure away from not existing.
4. **Security: `public.rls_auto_enable()` is SECURITY DEFINER and executable by `anon` and `authenticated`** via REST RPC. Proposed migration (awaiting HITL approval — NOT executed):
   ```sql
   revoke execute on function public.rls_auto_enable() from anon, authenticated, public;
   ```

## Warnings (non-blocking — schedule)

- `sites.owner_id` foreign key unindexed → `create index on public.sites (owner_id);`
- Both RLS policies re-evaluate `auth.uid()` per row → wrap as `(select auth.uid())` (perf at scale)
- No Uptime Kuma / Sentry (configs now prepared in `infra/docker/`, deploy pending)
- Ollama decommission still pending on VPS (unverifiable from session)

## Passed Checks

- Both existing tables have RLS **enabled with owner-scoped `auth.uid()` policies** — standard honoured
- `touch_updated_at` triggers present on both tables; search_path pinned by migration `20260710213636`
- Migration history clean (2 migrations, 2026-07-10)
- Brand tokens: no hardcoded hex outside token-definition files in repo
- No secrets committed; `.gitignore` blocks `.env`

## Recommended Actions (prioritised)

1. **CEO: locate or declare the Umsavati production project.** Everything else in the PRS hinges on whether the 25-table platform exists somewhere reachable.
2. Approve the `rls_auto_enable` revoke migration (HITL) — 1 statement, closes both security lints.
3. Deploy `infra/docker/uptime-kuma.compose.yml` + `glitchtip.compose.yml` via Coolify; wire Telegram alerts.
4. Bring product source into version control (this repo or dedicated repos under the org).
5. Decide POPIA lawful basis (memo at `templates/legal/POPIA_LAWFUL_BASIS_MEMO.md`) and approve consent-ledger migration.
6. Keep-alive or upgrade the Archon backend so it cannot silently re-pause.
7. Re-run /XRAY after 1–6; realistic path back to PRS ≥ 85 runs through evidence, not documentation.

---
---

# ⚡ XRAY RE-SCORE — SYSTEM (post foundation-schema apply)
**Status**: 🔴 RED (improved)
**Scanned**: 2026-07-20 (same day, after CEO HITL approval of the foundation schema)
**Method**: `umsavati_foundation` migration applied to `lpafkclumhhwsvgxrkwv`; live verification via advisors, catalog queries, and band-arithmetic probe.

## PRS = 38 / 100 → CRITICAL band (< 45) · **+12 vs morning audit**
Gate threshold ≥ 85. **Sprint 9 remains GATED.**

| Dimension | Weight | Was | Now | Evidence |
|-----------|--------|-----|-----|----------|
| Data layer & security | 25 | 8 | **14** | 11 tables live, RLS on 100%, 10 policies (all initplan form), 10 triggers, zero WARN lints, 6 migrations under version control. Still: 0 Edge Functions, 0 auth users, 0 rows, free-tier pause risk |
| Application surface | 25 | 3 | **3** | Unchanged — no mobile/web/SafeFile source in version control. **Now the dominant gap.** |
| Infrastructure & monitoring | 20 | 4 | **6** | Uptime Kuma + GlitchTip compose files ready in `infra/docker/`; not yet deployed — no live alerting |
| Compliance & legal (POPIA) | 15 | 5 | **9** | D1 adopted (legitimate interest); consent ledger live, append-only verified; attorney review + operational consent flow pending |
| Revenue readiness | 15 | 6 | **6** | Unchanged |

## Foundation-apply verification evidence

- `list_tables`: 11 public tables, `rls_enabled: true` on every one
- Security advisors: **zero WARN** — sole notice is INFO `rls_enabled_no_policy` on `popia_consent_ledger` (deny-by-default by design)
- Performance advisors: **zero WARN** — only INFO `unused_index` on brand-new indexes (expected at 0 rows)
- Catalog probe: 2 generated stored columns on `compliance_scores`, 10 policies, 10 user triggers, 11 RLS tables
- Band arithmetic verified across all four bands: 87.9→`compliant`, 72.0→`conditionally_compliant`, 50.0→`non_compliant`, 19.0→`critical_risk` (weights 0.28/0.22/0.18/0.14/0.10/0.08 exact)
- Insert-probe deferred: `organisations.owner_id → auth.users` and the account has **0 auth users** — first real signup will exercise the FK chain

## Update (same day): first Edge Function live → PRS 38 → 40

`score-organisation` deployed to `lpafkclumhhwsvgxrkwv` (v1, ACTIVE, JWT
verification on) — the platform's first of 11 documented Edge Functions, and
the scoring engine's write path. Rubric v1 lives in a pure module with 22
passing unit tests (`products/umsavati-ohs/supabase/functions/score-organisation/`).
Ownership enforced via RLS with the caller's JWT; score rows written via
service role (history immutable to clients); `total`/`band` remain DB-generated.
HTTP smoke test unavailable from this session (network policy blocks
`*.supabase.co` from the sandbox); deployment verified via management API +
bundle build. **Application surface 3 → 5 · PRS = 40/100 — still CRITICAL, gate holds.**

## Update (same day): dashboard shell under version control → PRS 40 → 42

Umsavati OS web shell committed at `products/umsavati-ohs/web/` — Next.js 15
App Router, TS strict, Tailwind v4 + shadcn conventions, `@supabase/ssr` auth
(login + session middleware), Fontsource-hosted brand typography. All 11
documented pages exist as routes; the dashboard reads `compliance_scores`
server-side and renders `total`/`band` (display only — arithmetic stays in
the DB). Brand rule held: every colour defined once as `--umh-*`/`--ohs-*`
in `globals.css`, zero hex elsewhere. Verified: clean `next build`
(14 routes), production smoke test all-200, tokens + all three typefaces
confirmed in built CSS. **Application surface 5 → 7 · PRS = 42/100 — CRITICAL, gate holds.**

## Update (same day): Organisations module → PRS 42 → 43

First working module in the shell: organisations CRUD via server actions
(create/edit/confirm-guarded delete), plus the end-to-end scoring loop —
"Run compliance scan" invokes `score-organisation` server-side with the
user's session and renders score + dimension tiles + immutable history.
RLS is the authorisation layer throughout. Verified: clean build, smoke
test (list 200 with graceful unconfigured state; detail 404s cleanly on
bare checkout — fixed a 500 there). Live E2E still pends first auth user.
**Application surface 7 → 8 · PRS = 43/100 — CRITICAL, gate holds.**

## Update (same day): Appointments module → PRS 43 → 44

Second working module: the S.16 appointment chain. Per-organisation chain
coverage (`covered/required` with gold/steel badges, sector-aware — CR 5
for construction, SASREA for events, mirroring the scoring rubric's
`requiredDesignations`), current/expired/future status derivation using
the same rule the engine applies, record + confirm-guarded remove via
server actions. Feeds Legal Compliance (28% — the heaviest dimension).
Verified: clean build, smoke test 200 with graceful unconfigured state.
**Application surface 8 → 9 · PRS = 44/100 — CRITICAL, gate holds.**

## Update (same day): SafeFile Documents module → PRS 44 → 45

Third working module: the six-module SafeFile register. Per-organisation
module coverage (`full/partial/missing` badges mirroring the rubric's
100/50/0 documentCompleteness rule — display-only, scoring.ts is source of
truth), register form with validity + S.16(1) sign-off, toggle-signed and
confirm-guarded remove via server actions. Feeds Document Completeness
(22%). Storage uploads deferred to the SafeFile pipeline build. Verified:
clean build, smoke 200. **App 9 → 10 · PRS = 45/100 — CRITICAL, gate holds.**

## Update (same day): Incidents module → PRS 45 → 46

Fourth working module: the S.24/COID incident register. Per-organisation
reportable-on-time ratio over the trailing 12 months (mirrors the rubric's
incidentManagement window and injury/disease reportable set — display only,
scoring.ts is source of truth), outstanding-incident alert, S.24 on-time /
late / unreported / not-reportable per-row badges, tri-state deadline
capture (7d injury / 14d disease), COID claim ref, confirm-guarded remove.
Feeds Incident Management (18%). Verified: clean build, smoke 200.
**App 10 → 11 · PRS = 46/100 — CRITICAL, gate holds. Top 68% of the score
weighting (Legal 28 + Docs 22 + Incidents 18) is now user-drivable.**

## Update (same day): Contractors + Training + Physical Agents → PRS 46 → 48

The remaining three scoring-input modules, completing the set — **all six
compliance dimensions are now user-editable from the dashboard**:
- **Contractors (14%)**: per-org qualification average, CIDB + CR 5 badges,
  toggle-verify, no-contractors=100 note (mirrors contractorQualification).
- **Training (10%)**: per-person currency, current/lapsed badges,
  non-expiring support, no-records=0 note (mirrors trainingCurrency).
- **Physical Agents (8%)**: 24-month within-action-level ratio, stale flag,
  exceeds/within badges, no-current=50 note (mirrors physicalAgentExposure).
All display-only mirrors; scoring.ts remains source of truth. Verified: clean
build (all 11 dashboard routes), smoke 200. **App 11 → 13 · PRS = 48/100 —
CRITICAL, gate holds. Full vertical slice complete: signup → org → populate
all six dimensions → scan → banded score.**

## Path to PRS ≥ 85 (re-prioritised)

1. **Application surface (worth up to +20): build and version-control the product code** — Expo mobile app, Next.js dashboard, SafeFile generator (scoring Edge Function ✅ done). This is now the critical path.
2. Deploy Uptime Kuma + GlitchTip (CEO, VPS) — up to +10, and the Kuma monitor doubles as the Supabase keep-alive.
3. First scoring Edge Function + seed tenant → proves the engine live end-to-end.
4. Attorney sign-off on POPIA memo; wire n8n opt-out flow into the ledger.
5. Instrument the Gumroad→Umsavati funnel.
